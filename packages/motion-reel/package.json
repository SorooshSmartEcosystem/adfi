{
  "name": "@orb/motion-reel",
  "version": "0.0.0",
  "private": true,
  "description": "Motion-reel pipeline — code-as-video for Reels/Shorts/TikToks. Hand-tuned scene choreographies; brand tokens + content slots vary per business. Phase 1: scene HTML generators only (server rendering pipeline lands in Phase 1.5).",
  "exports": {
    ".": "./src/index.ts",
    "./scenes": "./src/scenes/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@orb/config": "workspace:*",
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
