{
  "name": "@orb/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens (and later shared components) for the ADFI monorepo",
  "exports": {
    ".": "./src/index.ts",
    "./tokens": "./src/tokens.ts",
    "./tailwind/preset": "./tailwind/preset.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@orb/config": "workspace:*",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3"
  }
}
